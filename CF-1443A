#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n,temp=0;
        cin>>n;
        for(int i=1;i<=n;i++)
        {
            cout<<(4*n)-temp<<" ";
            temp+=2;
        }
        cout<<endl;
    }
    return 0;
}
